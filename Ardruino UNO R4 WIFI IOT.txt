/* 
  Sketch generated by the Arduino IoT Cloud Thing "Untitled"
  https://create.arduino.cc/cloud/things/ed52f88c-faf9-4f8a-8585-53acb6de5007 

  Arduino IoT Cloud Variables description

  The following variables are automatically generated and updated when changes are made to the Thing

  String message;
  float rainSensorReading;
  int position;
  bool redLEDButton;

  Variables which are marked as READ/WRITE in the Cloud Thing will also have functions
  which are called when their values are changed from the Dashboard.
  These functions are generated with the Thing and added at the end of this sketch.
*/
#include "ArduinoGraphics.h"
#include "Arduino_LED_Matrix.h"
          ArduinoLEDMatrix matrix;
#include "thingProperties.h"
#include <Servo.h>
#define rainAnalog A0
        int rainPercent = 0;
        Servo myservo;


void setup() {
  // Initialize serial and wait for port to open:
   pinMode(rainAnalog, INPUT);
  
  Serial.begin(9600);
    matrix.begin();
    matrix.beginDraw();
    matrix.stroke(0xFFFFFFFF);
    matrix.textScrollSpeed(100);

    // add the text
    const char text[] = "    Hello World!    ";
    matrix.textFont(Font_5x7);
    matrix.beginText(0, 1, 0xFFFFFF);
    matrix.println(text);
    matrix.endText(SCROLL_LEFT);
  matrix.endDraw();

  // This delay gives the chance to wait for a Serial Monitor without blocking if none is found
  delay(1000); 

  // Defined in thingProperties.h
  initProperties();

  // Connect to Arduino IoT Cloud
  ArduinoCloud.begin(ArduinoIoTPreferredConnection);
  
  /*
     The following function allows you to obtain more information
     related to the state of network and IoT Cloud connection and errors
     the higher number the more granular information you’ll get.
     The default is 0 (only errors).
     Maximum is 4
 */
  setDebugMessageLevel(2);
  ArduinoCloud.printDebugInfo();
  pinMode(2, OUTPUT);
  myservo.attach(9);
}

void loop() {
  ArduinoCloud.update();
  // Your code here 
   int sensorValue = analogRead(rainAnalog);
   rainPercent = map(sensorValue, 0, 1020, 100, 0);
      rainSensorReading = rainPercent;
  if (rainSensorReading > 30) { // Threshold for rain detection
      myservo.write(180);
      message = " It's raining!!! ";
      digitalWrite(2,1);
        matrix.beginDraw();
          matrix.stroke(0xFFFFFFFF);
          matrix.textScrollSpeed(100);

          // add the text
          const char text[] = " RED IS ON! ";
          matrix.textFont(Font_5x7);
          matrix.beginText(0, 1, 0xFFFFFF);
          matrix.println(text);
          matrix.endText(SCROLL_LEFT);
          matrix.play(true);
        matrix.endDraw(); 
    } else {
        myservo.write(0);
          message = " Sky is clear!!! ";
          digitalWrite(2,0);
          matrix.beginDraw();
          matrix.stroke(0xFFFFFFFF);
          matrix.textScrollSpeed(100);

          // add the text
          const char text[] = " RED IS OFF! ";
          matrix.textFont(Font_5x7);
          matrix.beginText(0, 1, 0xFFFFFF);
          matrix.println(text);
          matrix.endText(SCROLL_LEFT);
        matrix.endDraw(); 
    }
    delay(1000); // Delay for stability
}
/*
  Since RedLEDButton is READ_WRITE variable, onRedLEDButtonChange() is
  executed every time a new value is received from IoT Cloud.
*/
void onRedLEDButtonChange()  {
  // Add your code here to act upon RedLEDButton change
/*  if(redLEDButton == 1) { //If switch is on
    //digitalWrite(2,1); //the LED turns on
    matrix.beginDraw();
      matrix.stroke(0xFFFFFFFF);
      matrix.textScrollSpeed(100);

      // add the text
      const char text[] = " RED IS ON! ";
      matrix.textFont(Font_5x7);
      matrix.beginText(0, 1, 0xFFFFFF);
      matrix.println(text);
      matrix.endText(SCROLL_LEFT);
    matrix.play(true);
  matrix.endDraw(); 
  }
  else if(redLEDButton == 0) { //If switch is off
   // digitalWrite(2,0); //the LED turns off
    matrix.beginDraw();
      matrix.stroke(0xFFFFFFFF);
      matrix.textScrollSpeed(100);

      // add the text
      const char text[] = " RED IS OFF! ";
      matrix.textFont(Font_5x7);
      matrix.beginText(0, 1, 0xFFFFFF);
      matrix.println(text);
      matrix.endText(SCROLL_LEFT);
    matrix.endDraw(); 
  } */
}

/*
  Since Position is READ_WRITE variable, onPositionChange() is
  executed every time a new value is received from IoT Cloud.
*/
void onPositionChange()  {
  // Add your code here to act upon Position change
  
  myservo.write(position);
 
  if(position > 20) { //If switch is on
    digitalWrite(2,1); //the LED turns on
      matrix.beginDraw();
      matrix.stroke(0xFFFFFFFF);
      matrix.textScrollSpeed(50);

      // add the text
      const char text[] = " OPEN! ";
      matrix.textFont(Font_5x7);
      matrix.beginText(0, 1, 0xFFFFFF);
      matrix.println(text);
      matrix.endText(SCROLL_LEFT);
    matrix.play(true);
  matrix.endDraw(); 
  }
  else if(position < 20) { //If switch is off
    digitalWrite(2,0); //the LED turns off
    matrix.beginDraw();
      matrix.stroke(0xFFFFFFFF);
      matrix.textScrollSpeed(50);

      // add the text
      const char text[] = " CLOSE! ";
      matrix.textFont(Font_5x7);
      matrix.beginText(0, 1, 0xFFFFFF);
      matrix.println(text);
      matrix.endText(SCROLL_LEFT);
    matrix.endDraw(); 
  
}
}






/*
  Since RainSensorReading is READ_WRITE variable, onRainSensorReadingChange() is
  executed every time a new value is received from IoT Cloud.
*/
void onRainSensorReadingChange()  {
   // Add your code here to act upon RainSensorReading change
}



/*
  Since Message is READ_WRITE variable, onMessageChange() is
  executed every time a new value is received from IoT Cloud.
*/
void onMessageChange()  {
  // Add your code here to act upon Message change
 message = message;
}
